# Expo(React Native) のネイティブビルド（CocoaPods / Gradle autolinking）が
# pnpm の分離 node_modules では require.resolve に失敗するため、フラット配置にする。
# 例: Android の settings.gradle が require.resolve('@react-native/gradle-plugin') を解決できる。
node-linker=hoisted
