// domain/user — アカウント削除の原子的コミット用ポート。
// 牌譜・半荘・何切る（回答含む）・ユーザーを **1トランザクション** で消す。
// 個別リポジトリの delete を順に呼ぶと、途中失敗で中途半端に消えた孤児が残る
//（FK は削除の順序を守らせるだけで、部分失敗を巻き戻してはくれない）。

export interface AccountStore {
  /** 指定ユーザーの全データとユーザー自身を1トランザクションで削除する。 */
  deleteAll(userId: string): Promise<void>;
}
